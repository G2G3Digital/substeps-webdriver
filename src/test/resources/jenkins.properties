wait.seconds=10
base.url=src/web
driver.type=HTMLUNIT
#driver.type=FIREFOX
# only enable visual webdrivers if CI server has browsers installed and can run them headless

default.webdriver.timeout.secs=10